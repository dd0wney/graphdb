name: Lint

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

permissions:
  contents: read

jobs:
  golangci-lint:
    name: golangci-lint
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Set up Go
      uses: actions/setup-go@v6
      with:
        go-version: '1.26.4'
        # setup-go's cache stays on: golangci-lint-action v5+ caches only
        # its own analysis data and delegates Go module/build caching here.

    - name: Run golangci-lint
      # @v4-@v6 only support golangci-lint v1. The repo's
      # .golangci.yml is v2 format, so we need @v7+ which added
      # golangci-lint v2 support. Pin to v2.11.4 — the dev
      # environment's version, verified with `config verify`
      # locally. "latest" bumps to v2.x.y with stricter schema
      # validation that rejects fields the repo's config uses.
      uses: golangci/golangci-lint-action@v7
      with:
        version: v2.11.4
        args: --timeout=5m
        only-new-issues: false
        skip-cache: false

  go-mod-tidy:
    name: Check go.mod and go.sum
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Set up Go
      uses: actions/setup-go@v6
      with:
        go-version: '1.26.4'

    - name: Check if go.mod is tidy
      run: |
        go mod tidy
        git diff --exit-code go.mod go.sum
        if [ $? -ne 0 ]; then
          echo "❌ go.mod or go.sum needs tidying"
          echo "Run: go mod tidy"
          exit 1
        fi
        echo "✅ go.mod and go.sum are tidy"

  security:
    name: Security Scan
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Run Gosec Security Scanner
      # Pinned: @master is a mutable ref (upstream pushes change CI
      # behavior with no diff here to bisect against).
      uses: securego/gosec@v2.27.1
      with:
        args: '-no-fail -fmt=json -out=results.json ./...'

    - name: Upload security results
      uses: actions/upload-artifact@v7
      if: always()
      with:
        name: security-scan-results
        path: results.json
        retention-days: 30
