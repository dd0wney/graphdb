{
  "project": "graphdb",
  "tasks": [
    {
      "id": "v1.1-coi-screen-validation",
      "track": "v1.1",
      "status": "done",
      "title": "Real-corpus coi-screen validation (~814K ICIJ): exercise mmap end-to-end + answer B-1 (is full-graph enumeration a hot path?)",
      "closing_prs": [
        444
      ]
    },
    {
      "id": "v1.1-mmap-oracle-property-based",
      "track": "v1.1",
      "status": "done",
      "title": "Harden the JSON<->mmap equivalence oracle to property-based/fuzzed coverage (precondition for mmap-default in v1.2)",
      "closing_prs": [
        440
      ]
    },
    {
      "id": "v1.1-otel-tracing",
      "track": "v1.1",
      "status": "done",
      "title": "OpenTelemetry tracing + SLO/SLI docs (additive; pairs with existing Prometheus metrics)",
      "closing_prs": [
        442
      ]
    },
    {
      "id": "v1.1-reenable-fuzz-tests",
      "track": "v1.1",
      "status": "done",
      "title": "Re-enable the disabled fuzz tests (pkg/api/fuzz_test.go.disabled, pkg/query/fuzz_test.go.disabled)",
      "closing_prs": [
        441
      ]
    },
    {
      "id": "v1.3-gofmt-ci-gate",
      "track": "v1.3",
      "status": "done",
      "title": "CI: repo-wide gofmt -s lint gate. ROADMAP v1.3 says not started, but #490 added the gofmt -s step to test.yml and .golangci.yml sets gofmt.simplify",
      "closing_prs": [
        490
      ]
    },
    {
      "id": "v1.4-graphql-index-pagination",
      "track": "v1.4",
      "status": "pending",
      "title": "GraphQL index-level pagination: resolver offset -> ID cursor (REST side landed in #366)"
    },
    {
      "id": "v1.4-f3-compliance-http-api",
      "track": "v1.4",
      "status": "done",
      "title": "F3 compliance HTTP-API surface over the existing pkg/compliance framework (endpoints only) — already shipped May 2026 (#111/#114/#122/#124); re-listed in error, closed 2026-09-11 with PR 124"
    },
    {
      "id": "v1.4-sdk-parity",
      "track": "v1.4",
      "status": "pending",
      "title": "SDK parity: Python, TypeScript and Go clients catch up to the v1.4 endpoints"
    },
    {
      "id": "v1.1-coi-screen-real-corpus",
      "track": "v1.1",
      "status": "pending",
      "title": "Real-corpus coi-screen run (Milestone-1-proper, ~814K ICIJ). Needs the sibling coi-screen repo checked out beside graphdb and an ICIJ corpus on the machine; runbook in SPIKE_COI_SCREEN_VALIDATION_2026-07-01.md"
    },
    {
      "id": "v1.4-close-no-rewrite-when-clean",
      "track": "v1.4",
      "status": "pending",
      "title": "storage: Close must not rewrite the snapshot when the session wrote nothing. Surfaced by v1.1-coi-screen-real-corpus (#594): a read-only consumer rewrites the 1.1 GB snapshot.mmap on every exit (8.3 s / 7.9 GB; JSON 25 s / 17.5 GB). Acceptance: mtime and bytes unchanged after a read-only open/close, changed after one write, both seen red first; WAL truncate stays tied to a snapshot that happened. Spec row in NEXT_STEPS_2026-06-18.md section D"
    },
    {
      "id": "v1.4-json-close-no-rewrite-when-clean",
      "track": "v1.4",
      "status": "pending",
      "title": "storage: JSON-mode Close must not rewrite snapshot.json when the session wrote nothing. Same defect as v1.4-close-no-rewrite-when-clean (#598) on the other representation: a read-only consumer pays 25 s / 17.5 GB per exit on the 2.0M-node ICIJ store. JSON has no overlay, so the proof differs: WAL empty at open and LSN unchanged since, plus every unlogged mutation path (index definitions, SetEncryption, DeleteAllNodes) accounted for. BulkImportMode has no WAL and must always write. Acceptance: snapshot.json mtime and bytes unchanged after a read-only open/close, changed after one write, both seen red first; WAL truncate stays tied to a snapshot that happened"
    },
    {
      "id": "v1.4-wal-write-errors-reach-the-caller",
      "track": "v1.4",
      "status": "pending",
      "title": "storage: a WAL append failure must not be reported to the caller as success. Today enqueueWAL (9 sites, node and edge ops), waitWALPending (14 sites) and writeToWAL (6 sites, property and vector index definitions) print to stderr and return nil; the in-memory mutation stays, the WAL entry is missing, and until #602 Close could drop it. Decide and implement one contract: the caller gets an error and the in-memory change is rolled back, or the store refuses further writes until a snapshot lands. Acceptance: with a write fault on the WAL file, no public write method returns nil; a test per path family, each seen red first; replay after a crash never lacks a write the API confirmed. Interface change: propose the contract to the user before code"
    },
    {
      "id": "v1.4-api-wal-error-no-retry",
      "track": "v1.4",
      "status": "pending",
      "title": "api,graphql: map storage.ErrWALWriteFailed to a response that stops a client retry. Since v1.4-wal-write-errors-reach-the-caller a single-op write returns ErrWALWriteFailed with the entity applied in memory; the REST and GraphQL handlers surface it as a generic error, and a client that retries creates a second node. Acceptance: the REST status and body and the GraphQL error extension say the write applied and must not be retried, and carry the entity ID; one handler test per surface, each seen red first"
    },
    {
      "id": "v1.4-snapshot-records-wal-boundary",
      "track": "v1.4",
      "status": "pending",
      "title": "storage: a snapshot records the WAL boundary LSN it covers, and replay skips entries at or below it. Today a Close whose snapshot lands but whose WAL truncate fails for any cause (disk error at exit) leaves entries the snapshot already holds, and the next open replays them: a create from before a delete resurrects the deleted entity. Additive field in the JSON snapshot and in the mmap metadata tail (no version bump needed if absent means 0); replay compares entry.LSN to it. Acceptance: with the truncate forced to fail after a successful snapshot, a deleted node stays deleted after reopen, on both formats, seen red first"
    },
    {
      "id": "gate-private-netns-graphdb",
      "track": "infra",
      "status": "pending",
      "title": "ci,scripts: run the local go test gate inside a private network namespace so Portmaster cannot drop loopback SYNs. Signature: pkg/auth/oidc TestOIDCHandler_Callback_Success timed out at 136 s on a loopback connect under go test ./pkg/... and passed alone. Reuse in_private_netns and netns_available from graphdb-coord scripts/lib/netns.sh (PR #45 there). Acceptance: with portmaster.service active, a closed loopback port is refused in under 1 s inside the wrapper; the gate runs green with zero dial timeouts; when unshare is refused the gate takes a WARN fallback and still runs the tests"
    },
    {
      "id": "go-client-no-retry-on-post",
      "track": "v1.4",
      "status": "pending",
      "title": "go-client: do not retry POST or PATCH on a 5xx (M-11 parity with the TS and Python clients); read the 202 applied-not-durable body from v1.4-api-wal-error-no-retry and return a typed ErrNotDurable with the entity id. Today clients/go/transport.go isRetryable is 429 or >= 500 and request() retries with no method check, default 2 retries, so a POST /nodes that fails after the write applied creates a second node"
    },
    {
      "id": "v1.4-boundary-lsn-downgrade-guard",
      "track": "v1.4",
      "status": "pending",
      "title": "storage: refuse the open when the recovered WAL LSN is above 0 and below the recorded snapshot boundary (a downgrade to a binary that resets the LSN wrote entries the skip would drop); a failed truncate gives recovered == boundary and never fires; decide what to do about a torn WAL tail that reads low; red first with the measured sequence"
    },
    {
      "id": "v1.4-ts-client-202-not-durable",
      "track": "v1.4",
      "status": "pending",
      "title": "ts-client: a 202 applied-not-durable response is not a success. Today the TypeScript client returns the body as a normal result and drops the applied and durable fields, so the caller cannot tell a durable write from one that only applied in memory. Same shape as #618 for the Go client: read the 202 body, return a typed NotDurableError that carries the entity id, and never retry it. Acceptance: one test per write method family sees the typed error on a 202, each seen red first; a 201 path is unchanged"
    },
    {
      "id": "v1.4-python-client-202-not-durable",
      "track": "v1.4",
      "status": "pending",
      "title": "python-client: a 202 applied-not-durable response is not a success. Today the Python client returns the body as a normal result and drops the applied and durable fields, so the caller cannot tell a durable write from one that only applied in memory. Same shape as #618 for the Go client: read the 202 body, raise a typed NotDurableError that carries the entity id, and never retry it. Acceptance: one test per write method family sees the typed error on a 202, each seen red first; a 201 path is unchanged"
    },
    {
      "id": "v1.4-wal-backend-switch-policy",
      "track": "v1.4",
      "status": "pending",
      "title": "storage,docs: decide the contract for a WAL backend switch on one data directory. Since #619 the open refuses, which is loud, but no repair or migration path exists. Decide with the user whether the refusal is the documented contract or a migration is necessary. Acceptance: either docs name the refusal, its error, and the operator repair steps, or a migration opens the directory after a switch with a test seen red first. Small: docs or code"
    },
    {
      "id": "v1.4-wal-sync-failure-rollback",
      "track": "v1.4",
      "status": "pending",
      "title": "storage: poison the WAL after a sync failure. Today a sync failure leaves the LSN counter advanced while the bytes may not be durable, so the recorded snapshot boundary can exceed the durable LSN and a later open refuses with ErrWALBehindSnapshot (#619 residual). Decision: after a sync failure every later Append returns an error; Close still writes the snapshot and truncates the WAL, so no LSN is reused and the boundary never runs ahead of durable bytes. Acceptance: force a sync failure after a successful flush, then append (error), close, reopen (opens clean), seen red first"
    },
    {
      "id": "v1.4-consumer-drive-skip-policy",
      "track": "infra",
      "status": "pending",
      "title": "scripts: consumer-drive.sh SKIP policy. On a SKIP the script prints a warning and exits 0 when CI is unset, and exits 1 when CI=true, so a local run stays usable and a CI run cannot pass on a skipped drive. Small, scripts only. Acceptance: a selftest shows the exit 1 under CI=true and the exit 0 without it, seen red first"
    }
  ],
  "deps": [
    {
      "from": "v1.4-sdk-parity",
      "to": "v1.4-graphql-index-pagination"
    },
    {
      "from": "v1.4-sdk-parity",
      "to": "v1.4-f3-compliance-http-api"
    },
    {
      "from": "v1.4-api-wal-error-no-retry",
      "to": "v1.4-wal-write-errors-reach-the-caller"
    },
    {
      "from": "go-client-no-retry-on-post",
      "to": "v1.4-api-wal-error-no-retry"
    },
    {
      "from": "v1.4-boundary-lsn-downgrade-guard",
      "to": "v1.4-snapshot-records-wal-boundary"
    }
  ]
}
