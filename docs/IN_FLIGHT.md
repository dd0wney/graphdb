# In-flight work claims

Source of truth for parallel-agent task claims. Updated via the `work-claim` skill (`.claude/skills/work-claim/SKILL.md`).

Each row is one claim. Agent claims a planning-doc task ID, works on it, and removes the row when the work PR lands (or abandons via a release PR).

| Task ID | Agent ID | Worktree / branch | Started (UTC) | Notes |
|---|---|---|---|---|

<!-- No active claims. -->
