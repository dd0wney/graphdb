# Next session prompt

> **Generated by**: `docs/internals/design/SESSION_HANDOFF_2026-06-03-0411Z.md`
> **Singleton file**: every `session-handoff` skill invocation overwrites this. The latest handoff is the source.
> **Usage**: paste the block below to the next Claude Code session in this repo.

---

Read this file first: `docs/internals/design/SESSION_HANDOFF_2026-06-03-0411Z.md`.
Then `docs/NEXT_STEPS_2026-06-03.md` § Critical path, then `CLAUDE.md` § "Orient first".

State: **Track P is fully done** (measured backlog items 1–3 + item-4 M4/M5/M6 + item-1 `RemoveNodeProperties` remainder). The **`Transaction` API was completed as a durable, tenant-aware Go primitive** (#277 spec + #278 persist helpers + #279 atomic batch-WAL + #280 `Commit` rewrite) — it previously bypassed WAL durability + tenant/vector/property indexes + stats + observers. `main` HEAD `ac63c3c`.

Default next task (unless the user redirects): **Track Q — consumer-driven correctness hardening** (selected 2026-06-03, not yet started). Start with **Q1**: the vector unit tests assert result *count*, not nearest-neighbour *ranking* — add recall/known-answer assertions to `pkg/vector` + the storage/REST vector paths (this is the root-cause gap that let two REST vector bugs through; see the 2026-06-02 reconciliation in `NEXT_STEPS_2026-05-15.md`). Then Q2/Q3 (drive `understand-graphdb` + `coi-screen` against `main`, fix every divergence in graphdb + pin it), then Q4 (consumer-contract harness). TDD; `-race` storage/vector; bench on a shared fixture (auto-memory).

Resolve or acknowledge first:
1. Merge #281 (planning reconciliation recording the Transaction completion) — docs-only, verified.
2. Transaction follow-ups deferred (spec § Out of scope): tx deletes (`tx.DeleteNode`/cascade — `deletedNodes`/`deletedEdges` are dead scaffolding for it), conflict detection, client-facing transaction API. Implement only when a caller needs them (the API has no non-test callers yet).
3. Track P tail M3/M7 (M7 API-deprecation decision → then M3 snapshot-format bump — memory `project_track_p_m3_m7_deferred`); batched-WAL default sweep; inherited #240/#241.

Also pending: CLAUDE.md line ~69 still says `forEachEdgeUnlocked` "(edge variants)" — #261 deleted it.

End the session by invoking the `session-handoff` skill.
