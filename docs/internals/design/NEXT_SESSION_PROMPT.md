# Next-session prompt

Generated by `SESSION_HANDOFF_2026-09-15-0302Z.md`. Singleton — each handoff overwrites it.

---

Read `docs/internals/design/SESSION_HANDOFF_2026-09-15-0302Z.md` first, and §5's first bullet before you trust anything about `ErrWALBehindSnapshot`.

1. Merge #629 (WAL backend-switch refusal) on green. Then `git checkout main && git pull --ff-only`, then `coord release --pr 629 graphdb:v1.4-wal-backend-switch-policy` with a lesson. **Nothing else closes that claim**, and it is held under agent id `graphdb-3d`.
2. Mark #629 done in `docs/NEXT_STEPS_2026-06-18.md`. The track-D row predates the defect and does not mention it.
3. **Do not touch PR #625 or `graphdb:v1.4-ts-client-202-not-durable`.** Session `graphdb-coord-8b` holds that claim. Message the coord session (`ListAgents` shows its name) before any coord write that is not a claim or release of your own task.
4. Ask the user the two §6 questions. The benchmark-CI one carries evidence in §5 and the obvious fix is the wrong one.
5. No graphdb coord task is pending after the release. Seed from §4 with the coord session, which owns the seed file.
6. Method note that paid for itself today: the last task was seeded as a documentation decision and was in fact silent data loss. **Probe a task's premise with a test before doing the work it asks for.** And prove a new gate fails in both directions, not just one.
7. Use `make test-local` for the local gate, and the pinned `golangci-lint@v2.13.2` invocation for CI's exact linter.
8. End with the `session-handoff` skill.
