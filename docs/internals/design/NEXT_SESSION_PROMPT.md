# Next session prompt

> **Generated by**: `docs/internals/design/SESSION_HANDOFF_2026-06-04-0304Z.md`
> **Singleton file**: every `session-handoff` skill invocation overwrites this. The latest handoff is the source.
> **Usage**: paste the block below to the next Claude Code session in this repo.

---

Read this file first: `docs/internals/design/SESSION_HANDOFF_2026-06-04-0304Z.md`.
Then `CLAUDE.md` § "Orient first", then `docs/NEXT_STEPS_2026-06-03.md` (NOTE: stale — predates this session's tenant sweep, batch parity, and vector fix; a refresh is overdue).

State: `main` HEAD `27c3316`. This session shipped 11 PRs (Track P tail M3+M7, a tenant-isolation sweep F1–F3, batch-path parity #304) and a silent-bug hunt that found a **CRITICAL bug — the HNSW vector index was lost on every restart**, fixed in **open PR #305**.

**First action — merge #305** (CRITICAL vector-persistence fix; advisor-gated, `-race` clean) when CI is green (base `main`, expect routine `UNSTABLE` benchmark-comment only).

**Then — the queued C/D/E bundle** ("replay + delete vector parity", the user's standing choice):
- C: WAL `replayDeleteNode`/`replayDeleteEdge` skip the per-tenant index → count drift on crash recovery.
- E: batch `executeDeleteNode` skips `RemoveNodeFromVectorIndexes` (a #304 gap).
- D: `RemoveNodeProperties` skips vector re-plan.
Use the CC6 test discipline: assert COUNTS not list-membership for index drift; vector tests assert search results; some bugs are hidden by reopen-self-heal (memory `feedback_in_memory_index_drift_test_design`).

**After the bundle:** no earned critical path — run a planning checkpoint (productization is the standing candidate) and refresh the planning doc to absorb this session + the silent-bug backlog.

**Standing discipline:** reachability-verify agent "HIGH" findings before believing them; RED-proof each fix; storage persistence tests must cover the post-WAL-replay path, not just clean Close→reopen. Invoke the relevant DSA skill before structure/algorithm decisions.

End the session by invoking the `session-handoff` skill.
