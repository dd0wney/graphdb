# Next session prompt

> **Generated by**: `docs/internals/design/SESSION_HANDOFF_2026-06-02-0323Z.md`
> **Singleton file**: every `session-handoff` skill invocation overwrites this. The latest handoff is the source.
> **Usage**: paste the block below to the next Claude Code session in this repo.

---

Read this file first: `docs/internals/design/SESSION_HANDOFF_2026-06-02-0323Z.md`.
Then `docs/NEXT_STEPS_2026-05-15.md` (§ Decision 9 Reconciliation 2026-06-02), then `CLAUDE.md` § "Orient first".

There is now a spike-grounded critical path: **Track P — shrink what `gs.mu` guards**, earned by `docs/internals/design/AUDIT_performance_saas_load_2026-06-02.md` (PR #253). The backlog is ordered by measured benchmark leverage.

First task (unless the user redirects): **Track P item (1) — the WAL group-commit fix.** `BatchedWAL.Append` (`pkg/wal/batched_wal.go:76`) parks on its flush channel while the caller holds `gs.mu.Lock`, so batching can never amortize fsync — measured 1.7–2.6× *worse* than the fsync default (corrects the 2026-05-06 audit). Fix: assign the WAL sequence number under `gs.mu`, then release `gs.mu` before the flush wait; fold in marshal-before-lock (audit M2). Use TDD; re-run `pkg/storage/bench_concurrent_write_test.go` to confirm writes now scale with tenant count. Race-test storage changes (`go test -race ./pkg/storage/ -count=3`).

Keep the order honest: the HNSW-in-critical-section finding (H2) is the audit's *novel* result but only ~2.5% of write cost today — it's the next ceiling after (1), not the first fix. Don't lead with it.

Open question to resolve or acknowledge first: dispose inherited PRs #240/#241 (carried since 2026-05-24), or go straight to Track P? Optionally break Track P into coord tasks (P1/P2/P3) before starting.

End the session by invoking the `session-handoff` skill.
