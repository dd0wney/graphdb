# Next-session prompt

Generated by `SESSION_HANDOFF_2026-09-13-0045Z.md`. Singleton — each handoff overwrites it.

---

Read `docs/internals/design/SESSION_HANDOFF_2026-09-13-0045Z.md` first.

1. Finish the mid-flight close-out if it is still open: merge #594 once its macOS test + benchmark jobs are green, run `coord release --pr 594 graphdb:v1.1-coi-screen-real-corpus`, merge #595, then `git checkout main && git pull --ff-only`. Nothing else closes the coord task.
2. `coord next --claim` should give `graphdb:v1.4-close-no-rewrite-when-clean`. Work on branch `v1.4/v1.4-close-no-rewrite-when-clean`. Read `COI_SCREEN_REAL_CORPUS_2026-09-13.md` § Findings 1 and `pkg/storage/persistence.go` `Close` → `snapshotWithBoundary` first. Red-first: a test that sees the snapshot mtime and bytes unchanged after a read-only open/close and changed after one write; watch it fail on current main before the fix. Keep the WAL truncate tied to a snapshot that actually happened.
3. Pre-flight: coord on :8090 must be up (`coord status`). A claimed task starts with a $10 budget and the hook refuses every tool at 95%; only the user can grant, with the `task:` prefix. Ask early if the task will need reviews.
4. If you touch the mmap path, run `go test -race ./pkg/storage/ -count=3 -timeout 300s` and the JSON↔mmap equivalence oracle in `mmap_reopen_test.go`.
5. End with the `session-handoff` skill. Report whether the red-first test actually went red, and what it printed.
