# Next-session prompt

Generated by `SESSION_HANDOFF_2026-08-30-0308Z.md`. Singleton — each handoff
overwrites it.

---

First: `go test ./pkg/... -short` exits 1 on `pkg/licensing` and that is
**pre-existing, not yours**. `TestTelemetryReporter_Start_Success` has a real
unsynchronised `callCount`; `-race` on clean `main` reports DATA RACE. CI never saw
it because `Makefile:75` scopes `RACE_PKGS` to eight of about forty-two packages.

Then the track: **ADR 0003 step 5, the fault sweep over the seven enumeration
methods.** #531 gave them an error return; the argument for doing that was that no
fault sweep could reach those 26 call sites without one. Until the sweep exists the
change bought a signature and not a gate. Read `pkg/vfs/vfstest/sweep_role.go` and
`pkg/storage/mmap_release_balance_test.go` for the pattern.

Off-path alternatives, all recorded with file:line in §5.1 of the handoff: F9 (the
JSON snapshot publish syncs neither the file nor the directory, and the comment
justifying that is wrong on both halves), F5 (`pkg/graphql` returns raw storage
errors carrying a byte offset while `pkg/api` withholds them), F6, F10.

Pre-flight: `make dccc-cover` then `make dccc`, not `make test-cover`. `gofmt -s`,
not `gofmt`. `make lint-local` exits 2 on `pkg/storage` — an environment limit, not
a result. Run each gate's `*-selftest` before trusting it, and make any new gate
fail on purpose before you believe it passed.

Close the session with the `session-handoff` skill.
