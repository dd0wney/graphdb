# Next-session prompt

Generated by `SESSION_HANDOFF_2026-09-13-0415Z.md`. Singleton — each handoff overwrites it.

---

Read `docs/internals/design/SESSION_HANDOFF_2026-09-13-0415Z.md` first.

1. If graphdb #603 is still open, merge it on green, then `git checkout main && git pull --ff-only`.
2. Resolve the two leftover local branches from §3: `v1.4/v1.4-sdk-parity` is 0 ahead of main, delete it; `docs/planning-sdk-parity-done` holds one unpushed commit from 2026-09-11 — ask the user whether to rebase it onto main and open its PR, or drop it.
3. graphdb has no pending coord task. Pick from the handoff's §4. The recommendation is item 3, fail-soft WAL writes, because it is the last place a caller is told a write was durable when it was not; seed it first with the `coord-task` skill (edit `coord/seed/graphdb.json`, `coord seed --dry-run --seed-file …`, read for exactly one create, message `graphdb-coord-*` before the apply, apply with `COORD_PROJECT=graphdb`), then `coord next --claim`. Scope it before code: list every caller of `enqueueWAL`, `writeToWAL` and `waitWALPending`, and say which API returns change.
4. Pre-flight: `coord status` must reach :8090. A claimed task starts at $10 and the hook refuses every tool at 95%; a storage task with one review round needs about $25 more. Ask early, with the `task:` prefix. A refused tool call ran nothing — after a grant, re-check what the refused call was meant to do.
5. If you touch `pkg/storage`, run `go test -race ./pkg/storage/ -count=3 -timeout 600s` and the pinned linter (`go run github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.13.2 run ./pkg/... ./cmd/...`).
6. End with the `session-handoff` skill. Report whether each new test went red first, and what it printed.
