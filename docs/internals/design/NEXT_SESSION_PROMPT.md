# Next-session prompt

> Generated by `SESSION_HANDOFF_2026-08-28-0540Z.md`. Singleton — each handoff overwrites this file.

Read `docs/internals/design/SESSION_HANDOFF_2026-08-28-0540Z.md`, then
`SQLITE_TESTING_SCORECARD.md` and `COUPLING_AND_INTERFERENCE.md`.

Before new work:

1. **Land #485 and #486** if they are still open. #485 migrates `pkg/btree` and
   `pkg/lsm` to the filesystem driver; #486 is the coupling analysis.
2. **Do not trust the local `golangci-lint`.** It fails typecheck on a Go 1.26
   stdlib file, which suppresses the analysers, so it reports nothing about the
   code. CI's lint job is the only working one.

Then the recommended track: **concurrent fault injection**. Every fault, crash
and sweep test built on 2026-08-28 is single-threaded — zero goroutines across
six test files — while `pkg/storage`'s 28 concurrent test files inject no
faults. Inject a fault while a background flush is mid-write and a reader is
mid-scan, and sweep that point. `-race` does not cover this: it finds
unsynchronised access, not wrong sequencing across an interface.

Second track if that is not wanted: `pkg/storage`'s driver migration, ADR 0002
stage 4, 179 sites, with `syscall.Mmap` needing separate treatment.

Rule for either: move the scorecard row in the same PR that moves the code.

Close out via the `session-handoff` skill.
