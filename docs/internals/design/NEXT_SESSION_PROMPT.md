# Next-session prompt

Generated by `SESSION_HANDOFF_2026-09-08-0137Z.md`, refreshed after the v1.4.0 release the same
day. Singleton — each handoff overwrites it.

---

First: **nothing is in flight.** `main` holds #578 (ADR 0001), #579, #580 and #581, and the tag
`v1.4.0` (`7faf8a4`) is released with signed assets and Docker images `1.4.0`/`latest`. The
coord daemon on :8090 runs that build with `GRAPHDB_REQUIRED_UNIQUENESS_RULES=claim_for_task=Claim`
and the rule registered (graphdb-coord PR #17). Read `SESSION_HANDOFF_2026-09-08-0137Z.md` §5
before you trust a carried claim.

Second: the recommended code track is **A5, the tenant boundary** (`NEXT_STEPS_2026-06-18.md`
§E): endpoints fall back to the default tenant when no tenant context is set
(`pkg/api/middleware_tenant.go`), and the search index is not tenant-scoped
(`pkg/search/tenant_indexes.go`). Acceptance: a request with no tenant context is refused, and a
search never returns another tenant's rows. Run `/orchestrate` first (security is the lens).

Third, smaller: the roadmap's "v1.4.0 — Finish the API surface" items ship under a later minor;
a planning checkpoint should renumber them. `wiki-graphdb.service` on the dev box is in a
restart loop (encrypted snapshot, no key in the unit) and is unrelated to this repo's code.

Tier policy: pass `model` to `general-purpose`, `Explore` and `Plan`, or the Agent hook refuses
the call. Sonnet implements from a written brief with red-first tests; haiku runs sweeps and
returns raw output; opus reviews persisted-state and security-relevant diffs. The Bash tool waits
for every descendant process, so start a daemon only in a `run_in_background` call.

Pre-flight: Fable 5.1 pinned in `settings.json`. Go 1.27.0. Lint with
`go run github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.13.2 run ./pkg/... ./cmd/...`.
`gofmt -s`, never plain `gofmt`. Build `./pkg/... ./cmd/...`, never `./...`.

Close the session with the `session-handoff` skill.
