# Next-session prompt

Generated by `SESSION_HANDOFF_2026-08-28-1217Z.md`. Singleton — each handoff
overwrites it.

---

Start ADR 0002 stage 4: put `pkg/storage` on the `pkg/vfs` driver. 179 call
sites. Treat it as a track and plan it before writing code — the
`syscall.Mmap` path does not fit `vfs.File` and is explicitly out of scope for
this stage.

Validation angle: this is the first real use of the gates that landed on
2026-08-28. Run `make contract-guard`, `make dccc`, `make coverage-floors` and
`make lint-local` as you go, not at the end, and report which of them caught
something and which were noise. `make dccc` will move as `pkg/storage`'s
couplings gain tests.

Pre-flight: `gofmt -s`, not `gofmt`, is the format gate. The local
`golangci-lint` cannot run `govet` or `staticcheck` — `make lint-local` runs the
other nine. Mutation testing defaults to two workers and four exhausts this
machine.

Open decision before any registry work: `SESSION_HANDOFF_2026-08-28-1217Z.md`
§7 question 1, the ADR 0001 appendix.

Close the session with the `session-handoff` skill.
