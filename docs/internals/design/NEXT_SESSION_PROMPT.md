# Next-session prompt

Generated by `SESSION_HANDOFF_2026-09-15-0302Z.md`. Singleton — each handoff overwrites it.

---

Read `docs/internals/design/SESSION_HANDOFF_2026-09-15-0302Z.md` first.

1. If #627 (planning doc) is open, merge it on green, then `git checkout main && git pull --ff-only`.
2. **Do not touch PR #625 or `graphdb:v1.4-ts-client-202-not-durable`.** Session `graphdb-coord-8b` holds that claim. Message the coord session (`ListAgents` shows its name) before any coord write that is not a claim or release of your own task.
3. Ask the user the three §6 questions before starting work. The benchmark-CI one is the highest value: the evidence is in §5 and the fix is not the obvious one.
4. Then claim `graphdb:v1.4-wal-backend-switch-policy`, the only pending graphdb task. It is a design call first (document the refusal as the contract, or build a migration), so settle it with the user before writing code.
5. Use `make test-local` for the local gate, and the pinned `golangci-lint@v2.13.2` invocation for CI's exact linter.
6. Tier note: a sonnet subagent died mid-run with HTTP 403 `oauth_org_not_allowed` on 2026-09-15 when the plan changed to Pro. If that recurs, do the work in the main loop rather than retrying the tier, and say so.
7. End with the `session-handoff` skill.
