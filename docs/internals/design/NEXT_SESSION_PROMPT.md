# Next session prompt

> **Generated by**: `docs/internals/design/SESSION_HANDOFF_2026-06-01-1037Z.md`
> **Singleton file**: every `session-handoff` skill invocation overwrites this. The latest handoff is the source.
> **Usage**: paste the block below to the next Claude Code session in this repo.

Start the **int8-quantized HNSW** track — the evidence-backed pivot from the shelved SIMD track (float32 cosine is memory-bandwidth-bound; quantization cuts the bytes that are the actual bottleneck).

1. Read `SESSION_HANDOFF_2026-06-01-1037Z.md` first, then the **OUTCOME banner** atop `PERF_SIMD_ROADMAP_2026-06-01.md` (not the body — it's pre-evidence).
2. Brainstorm → plan → implement: store `int8` vectors + `float32` scale + `float32` norm per `hnswNode`; portable scalar int8 dot kernel (the spike kernel is in the handoff §5). Measure recall end-to-end on a real index before committing — confirm the spike's 0.00016 mean-error holds on real embeddings, not just random vectors.
3. Resolve the handoff's open questions FIRST (recall budget; int8 candidate-gen + float32 re-rank vs pure int8; archsimd-VNNI appetite on amd64). These shape the design.
4. Pre-flight: `go.mod` is now `go 1.26.0`; CI is on 1.26; `pkg/storage` full bench hangs on `DiskBacked_CacheMiss` (filter with `-bench`/`-run`). amd64 perf needs real CI runners, NOT local Docker (Rosetta distorts the compute/memory balance).
5. Don't re-propose SIMD-float32 — it's memory-bound, shelved with evidence. SIMD's only valid home here is int8-VNNI on amd64.
6. Close out via the `session-handoff` skill.
