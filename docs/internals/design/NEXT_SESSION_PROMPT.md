# Next-session prompt

Generated by `SESSION_HANDOFF_2026-08-29-1203Z.md`. Singleton — each handoff
overwrites it.

---

First, land the stack. Four pull requests are open and none is merged: #511
(base `main`, 10/10 green), #512 (base `fix/storage-absent-versus-refused`),
#513 (base `fix/storage-unreadable-single-record`), and #514 (base `main`,
green, independent).

**Retarget #512 to `main` before merging #511, and #513 to `main` before merging
#512.** `gh pr merge --delete-branch` on a stack-bottom PR auto-closes its
dependents and GitHub refuses to reopen them — `CLAUDE.md` records this.

**Retargeting is also what gives them CI.** Every workflow except
`benchmark.yml` filters `pull_request` to `[main, develop]`, so #512 has run one
check and #513 has run none. Do not read their current state as verified.

Then the track: **a damaged record survives at most one clean shutdown.**
`snapshotMmapLocked` DROPS a damaged node record where the edge path REFUSES
(`mmap_snapshot_persist.go:20` against `:43-51`). `Close()` calls `Snapshot`, so
one restart erases the directory entry and every diagnostic #512 adds goes quiet.
Read both branches before planning — the fix is to make the node side match the
edge side, and the asymmetry predates this work.

Eight further findings are in §5.1 of the handoff, ranked, none fixed.

Pre-flight: `make dccc-cover` then `make dccc`, not `make test-cover`. `gofmt -s`,
not `gofmt`. Run each gate's `*-selftest` before trusting it. Note that DCCC row
C4 measures the wrong function — finding 3.

Close the session with the `session-handoff` skill.
