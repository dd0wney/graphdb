# Next-session prompt

> Generated by `SESSION_HANDOFF_2026-08-28-0219Z.md`. Singleton — each handoff overwrites this file.

Read `docs/internals/design/SESSION_HANDOFF_2026-08-28-0219Z.md` first, then
`docs/NEXT_STEPS_2026-06-18.md` § "Follow-ups opened 2026-08-28".

Two things before new work:

1. **Check the nightly fuzz workflow ran.** `gh run list --workflow Fuzz`. It
   landed in #467 and its first execution is 14:17 UTC 2026-08-28. Nobody has
   seen it run. If it failed, the crashers are uploaded as artifacts and triaging
   them comes before anything else.
2. **Rebase and land #457.** It is green but its checks ran against a July base,
   and it touches `pkg/api/handlers_nodes.go`, which #470 changed. Re-run after
   the rebase.

Then pick up the recommended track: **an invariant checker for the mmap
representation.** #468 made `CheckInvariants` refuse mmap-backed stores instead
of falsely reporting health, so the default storage path (mmap since #447) has
no invariant checking — only the JSON↔mmap equivalence oracle. Build mmap-aware
ground truth from the membership section in `pkg/storage/mmap_snapshot_loader.go`.

Validation angle: this session added three gates (fault injection, nightly
fuzzing, coverage floor). Report what they caught or failed to catch on real
work, not just that they are present.

Pre-flight: the coord daemon on :8090 may still be running (key at
`~/.graphdb-coord-key`, data in `~/.graphdb-coord-data`). `golangci-lint` panics
locally on `pkg/graphql` and `pkg/api` for a toolchain reason that predates this
session — CI's Lint job is the real gate.

Close out via the `session-handoff` skill.
