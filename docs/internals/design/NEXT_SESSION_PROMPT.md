# Next-session prompt

Generated by `SESSION_HANDOFF_2026-09-13-1040Z.md`. Singleton — each handoff overwrites it.

---

Read `docs/internals/design/SESSION_HANDOFF_2026-09-13-1040Z.md` first.

1. If graphdb #607 or #608 is still open, merge it on green, then `git checkout main && git pull --ff-only`.
2. `coord next --claim`. Two tasks are pending; take `v1.4-snapshot-records-wal-boundary` (node 486) unless the user names the other. Work on branch `v1.4/v1.4-snapshot-records-wal-boundary`.
3. For node 486: read `pkg/storage/persistence_replay.go`, the `Close` truncate block in `pkg/storage/persistence.go`, and the mmap metadata tail in `pkg/storage/mmap_snapshot*.go`. Add one additive field to the JSON snapshot struct and to `mmapMetadata` for the WAL boundary LSN the snapshot covers (absent means 0, no version bump). Replay skips an entry whose LSN is at or below it. Keep the mmap clean-check normalisation in mind: the boundary is part of the metadata compare, and a clean session has the same boundary as at open. Red-first on both formats: a `vfstest` fault on the WAL truncate after a landed snapshot, then a deleted node must stay deleted after reopen. Watch it fail before the fix.
4. Pre-flight: `coord status` must reach :8090. A claimed task starts at $10 and the hook refuses every tool at 95%. Ask for a grant early with the `task:` prefix, and if the hook still refuses after a grant, ask for the same grant again.
5. Use the tiers: sonnet agents for the test file and the implementation from one written spec, opus reviewer before commit, the main loop for the contract, the red proof, and the gates.
6. If you touch `pkg/storage` or `pkg/wal`, run `go test -race ./pkg/storage/ ./pkg/wal/ -count=3 -timeout 600s` and the pinned linter (`go run github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.13.2 run ./pkg/... ./cmd/...`).
7. End with the `session-handoff` skill. Report whether each new test went red first, and what it printed.
