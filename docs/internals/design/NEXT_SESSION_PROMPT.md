# Next session prompt

> **Generated by**: `docs/internals/design/SESSION_HANDOFF_2026-06-02-0149Z.md`
> **Singleton file**: every `session-handoff` skill invocation overwrites this. The latest handoff is the source.
> **Usage**: paste the block below to the next Claude Code session in this repo.

---

Read this file first: `docs/internals/design/SESSION_HANDOFF_2026-06-02-0149Z.md`.
Then `docs/NEXT_STEPS_2026-05-15.md`, then `CLAUDE.md` § "Orient first".

Recommended next task: **Track R verification component (1c)** — exercise `GRAPHDB_AUTO_EMBED_ENABLED=true` in a Docker/k8s deployment under production-shaped node-create traffic, and confirm the auto-embedded vectors are actually **searchable** end-to-end.

Why now: newly worth doing. As of 2026-06-02, `main` finally supports correct REST vector ingestion (#246) AND correct HNSW search recall (#243). Before that, an auto-embed Docker run produced vectors that couldn't be ingested or ranked correctly — so (1c) now validates a real searchable result, not just "the observer fired."

Alternatives if (1c) isn't the priority:
- **(a)** Continue any `storage.Storage` migration / Track C tail (planner CALL test, `CallOperator` tests, `pkg/algorithms` widening) — mechanical, opportunistic.
- **(b)** Commission a new audit (perf under SaaS load / vector side-channels / multi-node scope).
- **(c)** Whatever the user names as higher priority.

Pre-flight:
- `JWT_SECRET` ≥32 chars; `ADMIN_PASSWORD` auto-creates admin; only the `default` tenant auto-creates.
- For an optional neural cross-check, point `GRAPHDB_EMBED_URL` at any OpenAI-compatible embeddings server (local Ollama `nomic-embed-text` works; avoid OpenAI per user constraint).
- Do NOT benchmark HNSW construction with uniform/zero synthetic vectors — that's the O(N²) worst case for every HNSW (issue #248); use real or clustered embeddings.

End the session by invoking the `session-handoff` skill. If you run (1c), include a short "what worked / what didn't" note on the end-to-end auto-embed→search path.
