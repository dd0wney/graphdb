# Next session prompt

> **Generated by**: `docs/internals/design/SESSION_HANDOFF_2026-06-04-0442Z.md`
> **Singleton file**: every `session-handoff` skill invocation overwrites this. The latest handoff is the source.
> **Usage**: paste the block below to the next Claude Code session in this repo.

---

Pick up from `docs/internals/design/SESSION_HANDOFF_2026-06-04-0442Z.md`.

**First**: the planning doc `NEXT_STEPS_2026-06-03.md` is stale (knows nothing of #300–#311). Refresh it (or write `NEXT_STEPS_2026-06-04.md`) from the handoff's §6 before choosing work — use the `planning-doc-update` skill.

**Recommended task — Phase C (metamorphic invariant test)**: the highest-value item this session earned. Apply the same op-script through the live / batch / transaction / WAL-replay paths and assert observationally identical results — crucially **vector search-result equality** (`SearchForTenant` top-k IDs), which closes the count-only limitation the #310/#311 checker can't see. New file `pkg/storage/invariant_metamorphic_test.go`, reusing `assertGraphInvariants`. Spec: `~/.claude/plans/we-need-improved-testing-bubbly-wave.md` § Phase C.

**Validation angle**: treat Phase C as a stress-test of the #310 checker on multi-path divergence — if expressing cross-path equality is awkward, that's signal about the checker's API.

**Alternative tracks** (if pivoting): extend the invariant checker to `propertyIndexes` + the FTS index (cheap; closes two more drift surfaces; turns "FTS lost on restart?" into a test); or the silent-bug backlog (FTS-on-restart, `CreateVectorIndex` WAL-logging); or productization.

**Housekeeping**: prior handoff PR #306 is still open (merge or close); run `branch-cleanup` for stale local branches (`feat/expose-*`, `perf/int8-hnsw`, `docs/session-handoff-2026-06-04-0304Z`).

**End** via the `session-handoff` skill.
