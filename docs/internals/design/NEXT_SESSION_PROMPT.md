# Next session prompt

> **Generated by**: `docs/internals/design/SESSION_HANDOFF_2026-06-03-0833Z.md`
> **Singleton file**: every `session-handoff` skill invocation overwrites this. The latest handoff is the source.
> **Usage**: paste the block below to the next Claude Code session in this repo.

---

Read this file first: `docs/internals/design/SESSION_HANDOFF_2026-06-03-0833Z.md`.
Then `docs/NEXT_STEPS_2026-06-03.md`, then `CLAUDE.md` § "Orient first".

State: **Track Q is CLOSED (Q1→Q4).** `main` HEAD `63c6c38`. Q4 shipped a consumer-contract regression harness (`docs/CONSUMER_CONTRACTS.md`, `grep -rn "CONSUMER CONTRACT:" pkg/`, `scripts/consumer-drive.sh`). Track P, R, H also closed → **graphdb has no earned critical path.**

**First action:** merge **#292** (planning-doc edits closing Track Q; #289-era prompt + the planning doc still say "Q4 remaining" until it lands).

**Then — this is a planning-checkpoint, not a queued task.** There is no auto-promoted next track. Open `NEXT_STEPS_2026-06-03.md` and pick the next track, or commission a fresh audit (the pattern that earned Track P). Readiest candidates: **M7→M3 tail** (gated on the M7 public-API-deprecation decision — memory `project_track_p_m3_m7_deferred`); the **batch delete/update tenant-index gap** (#288's sibling, small); **live-consumer CI promotion** of `consumer-drive.sh` (blocked on consumer-repo reachability); the **real ~814K ICIJ corpus** run; productization; security audit.

**Standing discipline:** any storage test asserting edge/traversal or tenant-visibility must include a `Close()`→reopen under the default compression config (memory `project_q3_storage_persistence_bugs`). New consumer divergence → tagged contract test + `CONSUMER_CONTRACTS.md` row (the growth rule). Invoke the relevant DSA skill before structure/algorithm decisions.

Carried decisions: M7 (unblocks M3); inherited #240/#241; real ICIJ corpus.

End the session by invoking the `session-handoff` skill.
