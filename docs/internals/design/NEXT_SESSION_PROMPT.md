# Next session prompt

> **Generated by**: `docs/internals/design/SESSION_HANDOFF_2026-06-02-0544Z.md`
> **Singleton file**: every `session-handoff` skill invocation overwrites this. The latest handoff is the source.
> **Usage**: paste the block below to the next Claude Code session in this repo.

---

Read this file first: `docs/internals/design/SESSION_HANDOFF_2026-06-02-0544Z.md`.
Then `docs/internals/design/AUDIT_performance_saas_load_2026-06-02.md` § Recommendations, then `docs/NEXT_STEPS_2026-05-15.md` § Decision 9, then `CLAUDE.md` § "Orient first".

State: **Track P item (1) — the WAL group-commit fix — is substantially done.** All hot node + edge write paths now release `gs.mu` during the WAL durability wait, so batched writes group-commit (#255/#256/#257, all race-clean). Batched bench: g=16 dropped 10.49ms → 0.68ms.

Default next task (unless the user redirects): **Track P item (2) — stop full cross-tenant scans on label-absent reads** (audit H4 → M1). Unlabeled `MATCH` (`pkg/query/match_node.go:34`, `physical_plan.go:63,868`) and every GraphQL edge resolver (`pkg/graphql/edges_resolvers.go:45`, `pagination_resolvers.go:123`, …) call `GetAllNodesForTenant`/`GetAllEdgesForTenant`, which scan ALL tenants then filter/paginate in memory. Back them with per-tenant enumeration + index-level pagination; fix `countNodes` to read `len(index)` instead of cloning a bucket. Read-path refactor → its own multi-PR increment. Use TDD; race-test storage/query changes.

Resolve or acknowledge first (open questions in the handoff):
1. Make batched WAL the default now that group commit works? (latency-vs-throughput call).
2. Item (2) now, or finish the minor item-(1) remainder (`RemoveNodeProperties` + admin index ops) first?
3. Inherited #240/#241 — dispose or adopt?

Also pending (not this session): planning-doc reconciliation marking Track P item (1) ✅, and the `CLAUDE.md` `pkg/intelligence`-"absent" correction.

End the session by invoking the `session-handoff` skill.
