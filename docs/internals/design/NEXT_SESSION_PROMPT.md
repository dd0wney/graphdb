# Next-session prompt

Generated by `SESSION_HANDOFF_2026-09-08-0137Z.md`. Singleton — each handoff overwrites it.

---

First: **two PRs are open.** #578 (ADR 0001, the uniqueness-rules registry, 13 commits, both
opus reviews clean) and #579 (planning doc). Read their checks by SHA
(`gh api repos/dd0wney/graphdb/commits/<sha>/check-runs`), merge with `--delete-branch`, then
`git worktree remove .claude/worktrees/task9-uniqueness`. `main` is at `00e31f1`.

Second: **do not deploy a build with #578 to the coord daemon** until graphdb-coord (sibling
repo) sets `GRAPHDB_REQUIRED_UNIQUENESS_RULES=claim_for_task=Claim` and registers the rule in
`coord-bootstrap.sh` via `POST /admin/uniqueness-rules`. With an empty required list the new
build accepts duplicate claims silently. That graphdb-coord work is the recommended next task;
test it against a copy of `~/.graphdb-coord-data`, the live store the ADR names.

Third: **read §5 of the handoff before trusting a carried claim.** Two claims from the 09-07
handoffs were wrong (the default config has compression ON; the `admissible` frontier load is
unconditional), and one from the pre-#577 code comment named a method that does not exist.

Tier policy: always pass `model` to `general-purpose`, `Explore` and `Plan` — the Agent hook
now refuses the call otherwise. Sonnet implements from a written brief with red-first tests;
haiku runs sweeps and returns raw output (verify its labels); opus reviews persisted-state and
security-relevant diffs. The main loop rules and reviews.

Pre-flight: Fable 5.1 pinned in `settings.json`. Go 1.27.0. Lint with
`go run github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.13.2 run ./pkg/... ./cmd/...`.
`gofmt -s`, never plain `gofmt`. Build `./pkg/... ./cmd/...`, never `./...`.

Close the session with the `session-handoff` skill.
