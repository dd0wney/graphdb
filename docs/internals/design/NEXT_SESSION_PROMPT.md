# Next-session prompt

Generated by `SESSION_HANDOFF_2026-09-07-0438Z.md`. Singleton — each handoff overwrites it.

---

First: **merge state.** #570 (planning doc) and the 04:38 UTC handoff PR were left open for
your merge. Read their checks by SHA (`gh api repos/dd0wney/graphdb/commits/<sha>/check-runs`),
never from a watcher's exit code, and merge with `--delete-branch`. `main` is at `8d33c4c` (#569).

Second: **resolve or acknowledge §6 of the handoff.** The library-API versioning decision has
no new evidence (recommendation: version it when `oit-cyber/interrogate` ships against it, with
the `CC15` row). `main-prerebase-backup` has all four of its commits on `main` by content
(recommendation: delete it).

Then the recommended code track is **ADR 0001, the uniqueness-rules registry**
(`docs/adr/0001-uniqueness-rules-registry.md`, status proposed, design settled). Run
`/orchestrate` first per the global "Significant Features" rule, read the ADR's killed
alternatives before designing anything, and treat the registry-write exemption as a named
requirement. It adds persisted state to the data directory, so the format-stability rule applies
from the first commit.

Before trusting a carried claim, read §5. One observation there (`getEdgeIDsForNode` with edge
compression on an mmap store) is unprobed; settle it with a test before citing it.

Pre-flight: Fable 5.1 is pinned in `settings.json` (verified 2026-09-07). Go 1.27.0. Lint with
`go run github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.13.2 run ./pkg/... ./cmd/...`.
`gofmt -s`, never plain `gofmt`. Build `./pkg/... ./cmd/...`, never `./...`. `go test
./pkg/storage/ -short` took 28 s on the dev box, not the three minutes `CLAUDE.md` implies.

Close the session with the `session-handoff` skill.
