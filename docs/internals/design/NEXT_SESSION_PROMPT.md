# Next session prompt

> **Generated by**: `docs/internals/design/SESSION_HANDOFF_2026-06-04-0945Z.md`
> **Singleton file**: every `session-handoff` skill invocation overwrites this. The latest handoff is the source.
> **Usage**: paste the block below to the next Claude Code session in this repo.

---

Pick up from `docs/internals/design/SESSION_HANDOFF_2026-06-04-0945Z.md`.

**State**: `main` HEAD `1d0cb11` (#317), **unchanged** — nothing merged last session. **6 open PRs**: work PRs `#319` (jailgraph CC7–CC9, test-only), `#320` (vector-index WAL durability), `#321` (property-index partial-apply fix), `#322` (planning-doc disposition); plus `#318` (prior 0810Z handoff, now superseded) and `#323` (this handoff).

**FIRST**: triage/merge `#319–#322` (all green; #322 references #320/#321, land it with/after them) and resolve the handoff overlap — close `#318` in favour of `#323`. CI normal state is `UNSTABLE`-but-mergeable (benchmark comment-step perms); verify the failure set first. Then run `branch-cleanup` (6 stale branches will accumulate).

**Then choose with the user** (no forced critical path):
- **Property-index update-path partial-apply follow-up** — `#321` fixed create+replay only; `updatePropertyIndexes` + batch re-index share the root cause but have a `Remove("not found")` wrinkle. Narrow.
- **Productization** — `v0.4.0` unblocked consumer-pinning: Python SDK, 4 documented-but-unbuilt enterprise plugins, onboarding docs, single-node framing.

**BEFORE touching any write path**: `Transaction.Commit` is a live consumer path (memory `project_transaction_path_live_consumer`) — do not deprecate it. Full `pkg/storage` suite needs `-timeout 600s` run **alone** (it exceeds the 300s default on this machine; a 300s timeout under concurrent lint/race is contention, not a regression).

**End** via the `session-handoff` skill.
