# Next-session prompt

> Generated by `docs/internals/design/SESSION_HANDOFF_2026-07-17-0230Z.md` (singleton — overwritten by each session handoff). Read that handoff for full context.

```
The v1.3 Go client shipped (#458). First: check PR #459 (planning-doc reconciliation) — if
checks are green, squash-merge with --delete-branch (pre-authorized); if red, triage per
ci-status-triage. Then close out v1.3 (docs/ROADMAP_post_1.0.md): (a) the repo-wide gofmt
lint gate (root golangci-lint doesn't flag gofmt; the go-client workflow already gates
clients/go), and (b) publish dd0wney/graphdb:1.2.0 — the Helm chart's default install can't
pull without it. Housekeeping: ask the user about main-prerebase-backup (4 unmerged commits
incl. a TypeFloatArray→HNSW storage fix — rescue or delete?); mark v1.1/v1.2/v1.3 coord
tasks done once the coord daemon (:8090) is reachable. Pre-flight: clients/go is its own Go
module — run its tests from inside the directory; build the root with
`go build ./pkg/... ./cmd/...` (NOT ./... — gitignored enterprise-plugins/ breaks it
locally; CI is fine). End via session-handoff.
```
