# Next session prompt

> **Generated by**: `docs/internals/design/SESSION_HANDOFF_2026-06-03-0220Z.md`
> **Singleton file**: every `session-handoff` skill invocation overwrites this. The latest handoff is the source.
> **Usage**: paste the block below to the next Claude Code session in this repo.

---

Read this file first: `docs/internals/design/SESSION_HANDOFF_2026-06-03-0220Z.md`.
Then `docs/internals/design/AUDIT_performance_saas_load_2026-06-02.md` § M3 + § M7, then `docs/NEXT_STEPS_2026-05-15.md` § Decision 9, then `CLAUDE.md` § "Orient first".

State: **Track P is done through the clean portion of item (4).** Items (1) WAL group-commit, (2) cross-tenant read scans, (3) HNSW-out (#266), and item (4)'s M5 (#269), M6 (#271), M4 (#272) are all merged. `main` HEAD `ffb8d58`.

What remains of the audit backlog is the **decision-laden tail — neither is a drop-in, both need a design call BEFORE coding:**
- **M3** (label-index O(K) removal under `gs.mu`, twice per `DeleteNode`; bulk delete is O(N²)). The `map[uint64]struct{}` fix changes the *persisted* global index's value type → **snapshot format version bump**; a sorted-slice avoids the format change but keeps O(N) shift. **Decide: format bump vs sorted-slice.**
- **M7** (mis-framed in the audit). NOT a dead-code delete — the global `nodesByLabel`/`edgesByType` mirror has live tenant-blind readers (`FindNodesByLabel`/`FindEdgesByType`, `node_adjacency.go:57`) and is snapshot-persisted. Real shape: deprecate/migrate the tenant-blind `Find*` API, *then* drop the mirror. **Decide: is the tenant-blind `Find*` API still wanted?**

If neither appeals, Track P's measured-leverage work is effectively complete — open a planning checkpoint / fresh `NEXT_STEPS_<DATE>.md` and pick a new critical path. Use TDD; `-race -count=3` storage/vector changes; bench on a shared fixture (not per-size fresh graphs — auto-memory).

Resolve or acknowledge first:
1. Merge #273 (planning reconciliation marking item-4 clean portion done) — docs-only, verified, left unmerged per convention.
2. M3 / M7 design decisions above.
3. Batched-WAL default still deferred pending a FlushInterval sweep. Inherited #240/#241 still carried.

Lower-leverage carried follow-ups: resolver-level index-level pagination (rec #2's deferred half), item-(1) remainder (`RemoveNodeProperties` + admin index ops). Also: CLAUDE.md line ~69 still says `forEachEdgeUnlocked` "(edge variants)" — #261 deleted it.

End the session by invoking the `session-handoff` skill.
