# Next-session prompt

Generated by `SESSION_HANDOFF_2026-08-29-0256Z.md`. Singleton — each handoff
overwrites it.

---

First, land #507 and #508 if they are still open. #507 is ADR 0002 stage 4 plus
the DCCC statement-count floor and two mmap reader fixes. #508 publishes a
public page on merge — `docs/` deploys to GitHub Pages — so treat it as a
one-way action and check the `[paste the article URL]` placeholder first.

Then the track: **separate a refused allocation from a missing record.**
`getNode` returns `(nil, false)` for both, so under memory pressure a query
silently returns an incomplete result. `TestSnapshotReadPathUnderAllocationFailure`
measured 1 of 3 nodes vanishing under fail-once and 2 of 3 under fail-all-from.
The fix is a reason code instead of a bool across 8 internal call sites, and it
reaches the public error boundary. Plan it before writing code.

Validation angle: stage 4 shipped a driver that nothing has yet used in anger.
Concurrent fault injection for `pkg/storage` was blocked on it and is now
unblocked — item 2 in the handoff. Doubling up would exercise the driver on real
work.

Pre-flight: `make dccc-cover` then `make dccc`, not `make test-cover` — the
coupling measure has its own profile now. `gofmt -s`, not `gofmt`. `make
lint-local` runs nine of CI's eleven linters. Run each gate's `*-selftest`
before trusting it.

Close the session with the `session-handoff` skill.
