# Next session prompt

> **Generated by**: `docs/internals/design/SESSION_HANDOFF_2026-06-03-0534Z.md`
> **Singleton file**: every `session-handoff` skill invocation overwrites this. The latest handoff is the source.
> **Usage**: paste the block below to the next Claude Code session in this repo.

---

Read this file first: `docs/internals/design/SESSION_HANDOFF_2026-06-03-0534Z.md`.
Then `docs/NEXT_STEPS_2026-06-03.md` § Critical path, then `CLAUDE.md` § "Orient first".

State: **Track Q is open; Q1 is done** (#283 — known-answer nearest-neighbour identity assertions at the `pkg/api` REST handler + `pkg/storage` k>1 ordering; closes the count-only gap that let two REST vector bugs through). `main` HEAD `f570ddf`. Track P fully closed; `Transaction` completed as a durable tenant-aware primitive (#277–#280).

Default next task (unless the user redirects): **Track Q / Q2 — drive `understand-graphdb` against `main` end-to-end.** Re-run the consumer's ingest + Phase 2–3 queries; for every divergence, land a graphdb bugfix **plus** a graphdb-side regression test (the Q1 NN-identity tests are the template — assert *which* nodes, not counts). Then Q3 (`coi-screen` Milestone-1 against real ICIJ corpus) and Q4 (consumer-contract regression harness). TDD; `-race` on storage/vector; bench on a shared fixture (auto-memory).

Resolve or acknowledge first:
1. **Q2 pre-flight (ask the user)** — how to run `understand-graphdb` against a local graphdb: server launch command + embeddings backend (`GRAPHDB_EMBED_URL` / local Ollama). Consumer is at `../understand-graphdb`. Don't guess the run setup; this is why Q2 was deferred to a fresh session.
2. **M7 decision (user)** — is the tenant-blind `FindNodesByLabel`/`FindEdgesByType` API still wanted? Answering unblocks the M7→M3 Track-P tail. M3's structure is already resolved (hash set `map[uint64]struct{}` via `choosing-a-map-or-set`; sort-on-read for pagination — memory `project_track_p_m3_m7_deferred`).
3. Transaction follow-ups deferred (spec § Out of scope): tx deletes, conflict detection, client-facing API — only when a caller needs them. Plus: batched-WAL default sweep; inherited #240/#241 disposition.

**Standing instruction:** invoke the relevant DSA skill (`~/.claude/skills/choosing-*`, `applying-*` — all 14 live + fully tested) before any data-structure/algorithm decision. M7→`choosing-a-concurrent-structure`; HNSW→`choosing-a-priority-structure`/`choosing-a-graph-algorithm`.

Also pending (small): CLAUDE.md line ~69 still says `forEachEdgeUnlocked` "(edge variants)" — #261 deleted it.

End the session by invoking the `session-handoff` skill.
