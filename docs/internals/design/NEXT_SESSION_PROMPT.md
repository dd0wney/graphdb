# Next session prompt

> **Generated by**: `docs/internals/design/SESSION_HANDOFF_2026-06-03-0704Z.md`
> **Singleton file**: every `session-handoff` skill invocation overwrites this. The latest handoff is the source.
> **Usage**: paste the block below to the next Claude Code session in this repo.

---

Read this file first: `docs/internals/design/SESSION_HANDOFF_2026-06-03-0704Z.md`.
Then `docs/NEXT_STEPS_2026-06-03.md` § Critical path, then `CLAUDE.md` § "Orient first".

State: **Track Q is done through Q3.** `main` HEAD `b5fcbac`. This session: Q1 (#283), Q2 (#286 + consumer validation), Q3 (#287 + #288). Q3 found and fixed **two storage persistence bugs** via consumer-driving — edge adjacency lost on reopen under default compression (#287, also confirmed by Stór), and bulk-import data invisible to `*ForTenant` readers (#288).

**First action (cheap, do before new work):** a planning-doc-update PR marking **Q2 and Q3 done** in `NEXT_STEPS_2026-06-03.md` — #285 marked only Q1; lines 47 (Q2) and 48 (Q3) are stale. Exact edits in the handoff §6 item 1.

Default next task: **Q4 — generalize a consumer-contract regression harness** (the last Track Q item). The seed pins are #283/#286/#287/#288 — turn the recurring "consumer surfaced a bug" loop into standing graphdb-side contract tests so future breakage is caught in CI. Or close Track Q at Q3 if Q4 isn't wanted (resolve open question 1).

**Standing discipline (memory `project_q3_storage_persistence_bugs`):** any graphdb test asserting edge/traversal or tenant-visibility MUST include a `Close()`→reopen under the default compression config, or it misses the entire bug class this session found. Invoke the relevant DSA skill before any structure/algorithm decision.

Also teed up (user decisions): real ~814K ICIJ corpus run (synthetic Milestone-1 already proven); M7 API-deprecation (unblocks M3); #240/#241.

End the session by invoking the `session-handoff` skill.
