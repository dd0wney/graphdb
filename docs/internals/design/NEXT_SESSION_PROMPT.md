# Next-session prompt

Generated by `SESSION_HANDOFF_2026-08-31-1010Z.md`. Singleton — each handoff overwrites it.

---

First: **check the four open PRs** (#549 `MaxResults` doc, #550 H-3 crash measurement, #551
bare-star Cypher fix, #552 mutation working-tree guard). All were complete and in CI at session
end. The standing instruction was "merge in order on green". Read #551 before merging it — it is a
deliberate behaviour change on the versioned REST surface, recorded in `CHANGELOG.md`.

Second: **check whether the `pkg/backup` subagent finished.** Branch `feat/backup-vfs`, worktree
`.claude/worktrees/agent-a9b23df5b4d5e78fc`. RESUME it, do not restart —
as of 10:17Z it had uncommitted work that COMPILES but zero commits and no PR, so `git status`
in that worktree is the first thing to read. The hazard in it is
`archive.go:70`'s `filepath.Walk` rebuilt on `fs.ReadDir`: sort explicitly, and prove the
equivalence test can REPORT A DIFFERENCE, or "identical" and "cannot tell" are the same output.
When it merges, tell the `github.com/dd0wney/fault` session — it has agreed to sweep that package.

Then the track: **`V1-spike`** in `docs/NEXT_STEPS_2026-06-18.md` — a design spike for node-visited
traversal and an expansion-time predicate. Design only; no implementation is approved. Re-read
`pkg/query/match_path.go` first, its line numbers moved twice today.

Off-path, all in §4 of the handoff: the `CAPABILITIES:175` / `STABILITY_POLICY:46` contradiction,
`durable_rename_test.go` being blind to the rename it covers, the fixture-dependent midpoint flip
in `TestMmapReaderRejectsACorruptSnapshotFromTheDriver`, and the unmeasured CRC detection question.

Pre-flight: `gofmt -s`, not `gofmt`. `bash scripts/lint-local.sh <pkg>` before pushing — it caught
an `errcheck` finding CI would otherwise have bounced. Never read an exit code after a pipe. Build
`./pkg/... ./cmd/...`, never `./...`.

Close the session with the `session-handoff` skill.
