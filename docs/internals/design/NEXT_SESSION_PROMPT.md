# Next-session prompt

Generated by `SESSION_HANDOFF_2026-09-14-0202Z.md`. Singleton — each handoff overwrites it.

---

Read `docs/internals/design/SESSION_HANDOFF_2026-09-14-0202Z.md` first.

1. If #613 is open, merge on green; `git checkout main && git pull --ff-only`.
2. Message `graphdb-coord-6f` (or the coord session `ListAgents` shows) and ask whether the three seed entries exist: `v1.4-boundary-lsn-downgrade-guard`, `go-client-no-retry-on-post`, `gate-private-netns-graphdb`. Do not seed them yourself.
3. `coord next --claim`. Expected: `graphdb:v1.4-boundary-lsn-downgrade-guard`. Before code, resolve the torn-tail policy with the user (refuse vs warn). Red first with the measured sequence in the handoff §4 item 1.
4. Use every tier: sonnet Explore for the code map, main loop for the spec and review, sonnet for tests and code, opus reviewer before the PR, a sonnet worker for the STE pass on the PR body.
5. Add the one-line `WALBoundaryLSN` note to `CLAUDE.md` § "Snapshot format stability" in its own docs PR.
6. End with the `session-handoff` skill.
