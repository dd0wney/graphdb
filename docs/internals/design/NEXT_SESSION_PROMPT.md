# Next-session prompt

Generated by `SESSION_HANDOFF_2026-09-04-0511Z.md`. Singleton — each handoff overwrites it.

---

First: **confirm `main` CI.** `gh run list --branch main --limit 12`. `f4dcfd4` (#555, Go 1.27)
was green on all six workflows. `8be5497` (#556, dependency bumps) and `d6bcaae` (#558,
`pkg/backup` on `pkg/vfs`) were still running at handoff. Read the conclusions, not exit codes.

Second: **decide the four correctness defects** in `SESSION_HANDOFF_2026-09-04-0511Z.md` §4.1
(F-a cancel returns nil error; F-b PROFILE and WITH drop the truncation signal; F-c REST answers
500 and drops rows on truncation; F-d relationship-pattern properties are silently ignored on
MATCH). Recommended: three small red-first PRs, each with the failing output in the body.

Third: **V1-spike decisions D1–D10** in `docs/internals/design/SPIKE_V1_DEEP_PATH_TRAVERSAL_2026-09-04.md`
§7. User decision. If accepted, implement per §8 (about 190 LOC plus 450 LOC of tests), F-a to
F-c first (D7/D8).

Also: tell the `github.com/dd0wney/fault` session that #558 landed — it agreed to sweep `pkg/backup`.

Pre-flight: Go 1.27.0. Lint with `go run github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.13.2 run ./pkg/... ./cmd/...`.
`gofmt -s`, never plain `gofmt`. Build `./pkg/... ./cmd/...`, never `./...`. The coord daemon was
down; `coord-next` falls back to this file. In auto mode the classifier refuses `git worktree remove`
and force-push; 12 merged branches and 6 agent worktrees await `branch-cleanup` with user approval.

Close the session with the `session-handoff` skill.
