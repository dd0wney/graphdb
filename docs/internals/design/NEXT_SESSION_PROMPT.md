# Next-session prompt

Generated by `SESSION_HANDOFF_2026-09-14-0612Z.md`. Singleton — each handoff overwrites it.

---

Read `docs/internals/design/SESSION_HANDOFF_2026-09-14-0612Z.md` first.

1. If #620 is open, merge on green; `git checkout main && git pull --ff-only`.
2. No graphdb coord task is pending. Message the coord session (`ListAgents` shows its name) and propose seeds from the handoff §4: the WAL sync-failure residual first, then the TS/Python 202 handling, then the backend-switch migration decision. The coord session owns the seed file; do not seed yourself.
3. Ask the user the two §6 questions before the sync-failure task, because it is a design choice (poison the WAL after a sync failure, or record the last synced LSN separately).
4. Use every tier: sonnet Explore for the code map, main loop for the spec and the design call, sonnet for tests and code in a worktree, opus reviewer with a numbered question list before the PR, sonnet worker for the STE pass on the PR body.
5. Use `make test-local` for the local gate.
6. End with the `session-handoff` skill.
