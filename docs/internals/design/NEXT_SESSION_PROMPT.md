# Next-session prompt

> Generated by `SESSION_HANDOFF_2026-06-23-0759Z.md`. Singleton — overwritten by each handoff.

---

v1.0.0 GA is shipped and signed; the forward queue is `docs/ROADMAP_post_1.0.md`. Pick up **v1.1 "Validate & observe"** — the four tasks are seeded in coord (`coord status --track v1.1`).

Start with a **corpus-free** v1.1 task — `v1.1-mmap-oracle-property-based`, `v1.1-otel-tracing`, or `v1.1-reenable-fuzz-tests` — because `v1.1-coi-screen-validation` (the linchpin that gates B-1) needs a local ~814K ICIJ corpus that isn't available; confirm the corpus before taking it.

**Validation double-up**: coord was bootstrapped fresh this session and hasn't driven graphdb work yet — exercise the `coord-claim` skill end-to-end (`coord claim graphdb:v1.1-<task>` → work → `coord release`) and note anything that felt off.

**Pre-flight**:
- coord daemon must be up — if `coord status` errors: `bash ../graphdb-coord/scripts/coord-bootstrap.sh` from the graphdb repo.
- Any server work needs `JWT_SECRET` set (fail-closed).
- Env quirks this session: git-over-SSH (1Password agent) auth and the git-HTTPS read replica were both flaky — prefer the `gh` REST API for PR/merge/tag ops, and `git fetch https://github.com/dd0wney/graphdb.git main` to resync local `main` (which may lag).

Close out via the `session-handoff` skill. Include a quick "did coord-claim work cleanly?" note.
