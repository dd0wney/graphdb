# Next-session prompt

Generated by `SESSION_HANDOFF_2026-09-13-0153Z.md`. Singleton — each handoff overwrites it.

---

Read `docs/internals/design/SESSION_HANDOFF_2026-09-13-0153Z.md` first.

1. If graphdb #599 is still open, merge it on green, then `git checkout main && git pull --ff-only`.
2. graphdb has no pending coord task. Pick from the handoff's §4 — the recommendation is the JSON-mode clean Close (same defect as #598, other representation, 25 s / 17.5 GB per read-only exit). Seed it with the `coord-task` skill: edit `coord/seed/graphdb.json`, `coord seed --dry-run --seed-file …` and read for exactly one create, message `graphdb-coord-*` (find it with `ListAgents`) before the apply, apply with `COORD_PROJECT=graphdb`, then `coord next --claim`.
3. For the JSON-mode task: read `pkg/storage/mmap_snapshot_clean.go` and #598 first. The mmap proof is structural (overlay, tombstones, WAL LSN, metadata bytes); JSON has no overlay, so the proof must cover BulkImportMode, which skips the WAL. Red-first: a test that sees `snapshot.json` mtime and bytes unchanged after a read-only open/close on a JSON store, and changed after one write. Watch it fail on main before the fix.
4. Pre-flight: `coord status` must reach :8090. A claimed task starts at $10 and the hook refuses every tool at 95%; only the user can grant, with the `task:` prefix — ask early.
5. If you touch `pkg/storage`, run `go test -race ./pkg/storage/ -count=3 -timeout 600s` and the pinned linter (`go run github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.13.2 run ./pkg/... ./cmd/...`).
6. End with the `session-handoff` skill. Report whether the red-first test actually went red, and what it printed.
