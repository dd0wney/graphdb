# Next session prompt

> **Generated by**: `docs/internals/design/SESSION_HANDOFF_2026-06-02-0844Z.md`
> **Singleton file**: every `session-handoff` skill invocation overwrites this. The latest handoff is the source.
> **Usage**: paste the block below to the next Claude Code session in this repo.

---

Read this file first: `docs/internals/design/SESSION_HANDOFF_2026-06-02-0844Z.md`.
Then `docs/internals/design/AUDIT_performance_saas_load_2026-06-02.md` § M3–M7, then `docs/NEXT_STEPS_2026-05-15.md` § Decision 9, then `CLAUDE.md` § "Orient first".

State: **Track P items (1) WAL group-commit, (2) cross-tenant read scans (H4), and (3) HNSW-out (H2) are all done** (#255–#257, #259–#262, #266) — the audit's top three measured-leverage recommendations. `GetAllNodes/EdgesForTenant` are O(tenant); the HNSW insert runs off `gs.mu`.

Default next task (unless the user redirects): **Track P item (4) — index-structure + vector-read hygiene.**
- **M3:** label-index removal is an O(K) scan run twice per `DeleteNode` under `gs.mu` (`node_indexing.go:51`, `tenant_operations.go:57`) → `map[uint64]struct{}` or sorted slice. Bulk delete is O(N²) today.
- **M4:** `findNewEntryPoint` (`pkg/vector/hnsw_graph.go:107`) is an O(N) scan under `gs.mu` → O(1) max-layer candidate set.
- **M7:** drop the dead tenant-blind global `nodesByLabel`/`edgesByType` mirror — the per-tenant indexes now fully cover enumeration; confirm no live readers, then delete.
- **M5:** `sync.Pool` the HNSW visited set (`hnsw_search.go`). **M6:** cache query/stored norms (`distance.go`).
Likely a multi-PR increment. Use TDD; `-race -count=3` storage/vector changes; bench M3/M4 deltas on a shared fixture (not per-size fresh graphs — see auto-memory).

Resolve or acknowledge first (open questions in the handoff):
1. Merge #267 (planning reconciliation marking item 3 done) — docs-only, verified, left unmerged per convention.
2. Batched-WAL default still deferred pending a FlushInterval latency-vs-throughput sweep (unstarted).
3. Inherited #240/#241 — dispose or adopt?

Lower-leverage follow-ups below item (4): resolver-level index-level pagination (rec #2's deferred half), item-(1) remainder (`RemoveNodeProperties` + admin index ops). Also: CLAUDE.md line ~69 still says `forEachEdgeUnlocked` "(edge variants)" — #261 deleted it.

End the session by invoking the `session-handoff` skill.
