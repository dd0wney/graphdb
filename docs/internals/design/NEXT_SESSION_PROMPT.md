# Next-session prompt

Generated by `SESSION_HANDOFF_2026-09-15-0302Z.md`. Singleton — each handoff overwrites it.

---

Read `docs/internals/design/SESSION_HANDOFF_2026-09-15-0302Z.md` first, and §5's first bullet before you trust anything about `ErrWALBehindSnapshot`.

1. `git checkout main && git pull --ff-only` before reading or branching from main.
2. **Do not touch PR #625 or `graphdb:v1.4-ts-client-202-not-durable`.** Session `graphdb-coord-8b` holds that claim. Message the coord session (`ListAgents` shows its name) before any coord write that is not a claim or release of your own task.
3. Ask the user the two §6 questions. The benchmark-CI one carries evidence in §5, and the obvious fix is the wrong one.
4. **No graphdb coord task is pending.** Seed from §4 with the coord session, which owns the seed file. Top candidate: the benchmark CI step budget.
5. Method note that paid for itself today: the last task was seeded as a documentation decision and was in fact silent data loss. **Probe a task's premise with a test before doing the work it asks for.** And prove a new gate fails in both directions, not just one.
6. Use `make test-local` for the local gate, and the pinned `golangci-lint@v2.13.2` invocation for CI's exact linter.
7. End with the `session-handoff` skill.
