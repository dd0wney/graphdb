# Next-session prompt

Generated by `SESSION_HANDOFF_2026-08-30-1136Z.md`. Singleton — each handoff
overwrites it.

---

First: **merge #536**. It is open, CLEAN, all checks green, red-first evidence in
the body. It syncs the LSA snapshot publish, the third instance of a defect an
external crash sweep found in `pkg/storage`. Until it lands, `SaveToFile` calls
the os package directly and no fault or crash driver can observe it at all.

Second: `go test ./pkg/... -short` exits 1 on `pkg/licensing`, and that is
**pre-existing, not yours**. `TestTelemetryReporter_Start_Success` has a real
unsynchronised `callCount`; `-race` on clean `main` reports DATA RACE. CI never
saw it because `Makefile:75` scopes `RACE_PKGS` to eight of about forty-two
packages.

Then the track: **sweep the mmap snapshot publish.** The
`github.com/dd0wney/fault` session now ships optional `WriteAt` and `Seek`, so
`mmap_snapshot_writer.go:184` is reachable. That is graphdb's DEFAULT publish
path since v1.2 — the JSON path they already swept is the fallback. Their
harness is a throwaway module importing both repositories through `replace`, so
no dependency decision is needed. Reach them via `ListAgents`; they asked to be
told when #536 lands.

Off-path, all recorded with file:line in §5.1 of the handoff: F11 (the coverage
floors' reference run is two days and about 25 pull requests stale, so the real
margin is unknown — regenerate from a CI profile, never a local one), F10, F5,
F6, and ADR 0003 step 5.

Pre-flight: `make lint-local` covers errcheck and is the gate that was skipped
last session; run it. `gofmt -s`, not `gofmt`. `make dccc-cover` then `make
dccc`. Run each gate's `*-selftest`, and make any new gate fail on purpose
before you believe it passed — a red result proves a test can fail, but only
reading what it matched proves it failed for the right reason.

Close the session with the `session-handoff` skill.
