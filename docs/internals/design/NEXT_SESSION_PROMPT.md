# Next session prompt

> **Generated by**: `docs/internals/design/SESSION_HANDOFF_2026-06-02-0246Z.md`
> **Singleton file**: every `session-handoff` skill invocation overwrites this. The latest handoff is the source.
> **Usage**: paste the block below to the next Claude Code session in this repo.

---

Read this file first: `docs/internals/design/SESSION_HANDOFF_2026-06-02-0246Z.md`.
Then `docs/NEXT_STEPS_2026-05-15.md`, then `CLAUDE.md` § "Orient first".

First action: **merge PR #251** if it isn't already merged — the Track R component (1c) closure (auto-embed deployment verified searchable end-to-end). It bundles the harness + verification doc + the planning-doc update that closes (1c). Run `ci-status-triage` first; expect only the known-benign benchmark-comment-step `UNSTABLE`. After merge, no further planning-doc edits are needed for (1c) — they're already in the PR.

Then: the **Track R verification gap (1a + 1b + 1c) is fully closed** and there is no spike-grounded critical path. The planning doc's default-next is **(C) commission a new audit** — pick an angle (perf under realistic SaaS load / vector-embedding side-channels / multi-node scope) and document why in the PR. Don't manufacture a track without one of these.

Off-path (opportunistic): Track C tail — planner CALL test, `CallOperator` tests, `pkg/algorithms` `storage.Storage` widening. Inherited #240/#241 (another session's) still open.

One follow-up surfaced by (1c), not yet a task: a customer-facing **auto-embed deployment-ordering note** (create the vector + LSA indexes before searchable traffic) — productization-doc item, not code.

End the session by invoking the `session-handoff` skill.
