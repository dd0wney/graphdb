# Next session prompt

> **Generated by**: `docs/internals/design/SESSION_HANDOFF_2026-06-04-0810Z.md`
> **Singleton file**: every `session-handoff` skill invocation overwrites this. The latest handoff is the source.
> **Usage**: paste the block below to the next Claude Code session in this repo.

---

Pick up from `docs/internals/design/SESSION_HANDOFF_2026-06-04-0810Z.md`.

**State**: `main` HEAD `1d0cb11` (#317). No open PRs, no stale branches, tree clean. The parallel-invariant test harness is complete (label/type, counts, adjacency, vector count-only, propertyIndexes exact; FTS consciously out of scope). **`v0.4.0` is cut** — graphdb's first release of the full durability/persistence hardening wave.

**No critical path is forced.** Choose with the user:
- **Productization** (highest external value now that `v0.4.0` unblocked consumer-pinning): Python SDK, the 4 documented-but-unbuilt enterprise plugins, onboarding docs, single-node framing.
- **Small correctness backlog**: `CreateVectorIndex` not WAL-logged (crash-loses a post-snapshot index; `CreatePropertyIndex` already IS WAL-logged); LSA stale after WAL-replay.

**BEFORE touching any write path**: read stale-assumption #1 + memory `project_transaction_path_live_consumer`. `Transaction.Commit` is a **live, consumer-facing** path (stór's `Register`/`LinkRealisation`) — NOT dormant. Do not deprecate/refactor it on a no-callers assumption.

**Cross-repo (informational)**: stór will pin `v0.4.0` and move to M1.b (inter-process locking, stór-side). No graphdb action unless a new divergence surfaces.

**End** via the `session-handoff` skill.
