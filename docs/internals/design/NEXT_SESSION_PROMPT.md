# Next session prompt

> **Generated by**: `docs/internals/design/SESSION_HANDOFF_2026-06-04-2307Z.md`
> **Singleton file**: every `session-handoff` skill invocation overwrites this. The latest handoff is the source.
> **Usage**: paste the block below to the next Claude Code session in this repo.

---

Pick up from `docs/internals/design/SESSION_HANDOFF_2026-06-04-2307Z.md`.

**State**: `main` HEAD `c0b6dd6`. The Go **module is now `github.com/dd0wney/graphdb`** (renamed from `cluso-graphdb`, #335) and **`v0.4.1` is cut** (tag + GitHub release) — pin v0.4.1, NOT v0.4.0 (its go.mod has the old path). #328 (non-finite edge weight) fixed. One open PR: **#336** (planning-doc reconciliation, mergeable).

**FIRST**: merge #336 if not already; run `branch-cleanup` (two doc branches linger). CI normal state is `UNSTABLE`-but-mergeable (benchmark comment-step); verify the failure set first.

**No forced critical path.** Choose with the user:
- **Remaining surfaced gaps** (open issues): **#329** (OpenAPI `format: uint64`→`int64`, trivial), **#331** (`/traverse` ignores `direction`/`edge_types` — implement or remove).
- **Python SDK M2** — facades for hybrid-search/embeddings/`/v1/retrieve`/admin/tenants/apikeys (then M3 async, M4 caching/retry/LangChain). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-04-python-sdk-*.md`; memory `project_python_sdk`.

**Open questions for the user**: (1) rename the Docker image `cluso-graphdb`→`graphdb` too (Go module already renamed; Docker untouched)? (2) downstream import updates — `graphdb-enterprise` + `coi-screen` must move to `github.com/dd0wney/graphdb/...` + pin v0.4.1 (separate repos).

**Pre-flight**: SDK is uv-managed (`cd clients/python && uv sync`; `uv run pytest/ruff/mypy`). Full `pkg/storage` suite needs `-timeout 600s` run **alone** (~460–500s; a 300s timeout under concurrent lint/race is contention). Don't deprecate `Transaction.Commit` (live stór path); coordinate before renaming stór's API surface — memory `project_transaction_path_live_consumer`.

**End** via the `session-handoff` skill.
