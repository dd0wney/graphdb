# Next session prompt

> **Generated by**: `docs/internals/design/SESSION_HANDOFF_2026-06-01-1319Z.md`
> **Singleton file**: every `session-handoff` skill invocation overwrites this. The latest handoff is the source.
> **Usage**: paste the block below to the next Claude Code session in this repo.

---

Read this file first: `docs/internals/design/SESSION_HANDOFF_2026-06-01-1319Z.md`.
Then `docs/NEXT_STEPS_2026-05-15.md`.

Two stacked PRs from the prior session are open and unmerged:

- **#243** `fix(vector)`: HNSW search recall 0.0 → 1.0 (pre-existing P0 — search was broken for any index > ~ef). Base `main`.
- **#244** `feat(vector)`: int8-quantized HNSW (~0.99 recall on real GloVe, 4× memory) + amd64 SIMD dot. **Stacked** on #243.

Recommended next task (pick one):

- **(a)** Close out the stack: merge **#243 first**, then **retarget #244's base to `main` BEFORE** merging #243 with `--delete-branch` (or merge #243 without it) — stacked-PR gotcha, see `CLAUDE.md`. Then merge #244. Confirm the `simd-int8` CI job is green on a real amd64 runner and capture the int8-vs-scalar speedup.
- **(b)** Land a `planning-doc-update` PR adding the int8 track + the P0 HNSW finding to `docs/NEXT_STEPS_2026-05-15.md` (it currently reflects neither).
- **(c)** Whatever you (user) name as higher priority.

Pre-flight:

- CI normal state is `UNSTABLE` (benchmark comment-step permissions) — classify before merging per `CLAUDE.md`.
- Auto-memory `reference_graphdb_embedding_search_api` needs a note that HNSW recall was broken at scale pre-#243 (fixed by min-heap candidates + neighbour-selection heuristic).
- Two stashes exist; `stash@{0}` is the user's intact `enterprise_tests_framework.go` WIP (do not drop without asking).
- go1.26 `archsimd` does NOT expose true VNNI (`VPDPBUSD`) — only the AVX2 widen→VPMADDWD int8 dot. Don't plan SIMD assuming VNNI intrinsics.

End the session by invoking the `session-handoff` skill.
