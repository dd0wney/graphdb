# Next session prompt

> **Generated by**: `docs/internals/design/SESSION_HANDOFF_2026-06-05-1326Z.md`
> **Singleton file**: every `session-handoff` skill invocation overwrites this. The latest handoff is the source.
> **Usage**: paste the block below to the next Claude Code session in this repo.

The backlog is clear — **no open issues, no open PRs**, no critical path forced. Pick one:

1. **Python SDK M2** (the only remaining productization item) — ergonomic facades for hybrid-search / embeddings / `/v1/retrieve` / query / graphql / admin / tenants / apikeys. Start from `docs/superpowers/specs/2026-06-04-python-sdk-*.md` + memory `project_python_sdk`; M1 core (`clients/python/`) and the `_raw` escape hatch already exist. Use brainstorm→writing-plans→subagent-driven-development like M1.
2. **Operability polish** (godoc covered the library surface this session): a server/REST README quickstart + a `cmd/graphdb` deployment-ordering `doc.go` (JWT_SECRET required, admin bootstrap via ADMIN_PASSWORD, data dir).

**Pre-flight:** run the FULL affected-package suite locally before any PR — CI's `make test` now covers `storage/lsm/query/algorithms/parallel/wal/api/graphql` + `./cmd/...`, and `golangci-lint run` now catches gofmt everywhere (incl. cmd/). Run `pkg/storage` alone with `-timeout 600s` (~460s). Don't deprecate `Transaction.Commit` (live stór path) — memory `project_transaction_path_live_consumer`.

End the session with the `session-handoff` skill.
