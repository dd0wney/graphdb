# Next session prompt

> **Generated by**: `docs/internals/design/SESSION_HANDOFF_2026-06-02-1241Z.md`
> **Singleton file**: every `session-handoff` skill invocation overwrites this. The latest handoff is the source.
> **Usage**: paste the block below to the next Claude Code session in this repo.

---

Read this file first: `docs/internals/design/SESSION_HANDOFF_2026-06-02-1241Z.md`.

The 2026-06-02 12:41Z session built the **`coi-screen`** sibling consumer tool (private repo
`dd0wney/coi-screen`, local `../coi-screen`) and did **not** touch graphdb core. So graphdb's
critical path is exactly where the 08:44Z handoff left it, advanced only by `#269`.

## If your task is graphdb core (the default)

Orient: `docs/internals/design/SESSION_HANDOFF_2026-06-02-0844Z.md`, then
`docs/internals/design/AUDIT_performance_saas_load_2026-06-02.md` § M3–M7, then
`docs/NEXT_STEPS_2026-05-15.md` § Decision 9, then `CLAUDE.md` § "Orient first".

State: **Track P items (1)–(3) done** (#255–#257, #259–#262, #266); **item (4) M5 (HNSW
visited-set pooling) now also done via #269.** `GetAllNodes/EdgesForTenant` are O(tenant);
HNSW insert runs off `gs.mu`; HNSW search visited-set is pooled.

Default next task: **finish Track P item (4) — remaining index/vector hygiene (M5 done):**
- **M3:** label-index removal is an O(K) scan run twice per `DeleteNode` under `gs.mu`
  (`node_indexing.go:51`, `tenant_operations.go:57`) → `map[uint64]struct{}` or sorted slice.
  Bulk delete is O(N²) today.
- **M4:** `findNewEntryPoint` (`pkg/vector/hnsw_graph.go:107`) is an O(N) scan under `gs.mu` →
  O(1) max-layer candidate set.
- **M7:** drop the dead tenant-blind global `nodesByLabel`/`edgesByType` mirror — per-tenant
  indexes now cover enumeration; confirm no live readers, then delete.
- **M6:** cache query/stored norms (`distance.go`).
Likely multi-PR. TDD; `-race -count=3` on storage/vector; bench M3/M4 deltas on a shared
fixture (not per-size fresh graphs — see auto-memory).

Resolve or acknowledge first:
1. Inherited open PRs #240/#241 — dispose or adopt?
2. Batched-WAL default still deferred pending a FlushInterval latency-vs-throughput sweep.

## If your task is coi-screen (the side-thread)

It's a BUILT MVP — do not redesign. Read its `README.md`, the plan at
`docs/superpowers/plans/2026-06-02-coi-screen.md`, and memory `project_coi_screen_tool`. Only
open work is **Milestone-1-proper**: download the real ICIJ corpus (recipe in
`docs/internals/design/ICIJ_OFFSHORE_LEAKS_BENCHMARK.md`), import via `cmd/import-icij`, run
`coi-screen` to measure real resolution precision, and calibrate `--flag-threshold` /
`--max-hops` / `score.go` weights. Pre-flight: ICIJ CSVs (not in-repo) + graphdb checked out
as `../graphdb` for coi-screen's `replace` to resolve.

End the session by invoking the `session-handoff` skill.
