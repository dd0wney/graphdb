# Next session prompt

> **Generated by**: `docs/internals/design/SESSION_HANDOFF_2026-06-04-1242Z.md`
> **Singleton file**: every `session-handoff` skill invocation overwrites this. The latest handoff is the source.
> **Usage**: paste the block below to the next Claude Code session in this repo.

---

Pick up from `docs/internals/design/SESSION_HANDOFF_2026-06-04-1242Z.md`.

**State**: `main` HEAD `dd0a3b7`. Tree clean, no open PRs, only `main` branch. The **Python SDK M1 shipped** (`clients/python/`, #326/#327); edge update/delete + the `OpUpdateEdge` replay gap fixed (#332, closed #330).

**No forced critical path.** Choose with the user:
- **Close the surfaced gaps** (filed as issues): **#328** (±Inf/NaN edge weight breaks WAL persistence — real durability bug, highest value; guard `CreateEdge`/batch/storage + de-flake the storage stress test), **#329** (OpenAPI `uint64`→`int64`, trivial), **#331** (`/traverse` ignores `direction`/`edge_types`).
- **Python SDK M2** — ergonomic facades for the rest (hybrid-search/embeddings/`/v1/retrieve`/query/graphql/admin/tenants/apikeys), then M3 (async), M4 (caching/retry/LangChain). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-04-python-sdk-*.md`; memory `project_python_sdk`.

**Pre-flight**: SDK is uv-managed (`cd clients/python && uv sync`; `uv run pytest/ruff/mypy`). Full `pkg/storage` suite needs `-timeout 600s` run **alone** (~485s; exceeds the 300s default — a timeout under concurrent lint/race is contention, not a regression). Don't deprecate the `Transaction.Commit` path (live stór consumer — memory `project_transaction_path_live_consumer`).

**Also**: update `NEXT_STEPS_2026-06-03.md` (productization now M1-done) + refresh memory `project_python_sdk` (#330 closed; #328/#329/#331 open). PyPI publish stays inert until trusted-publishing + a `py-sdk-v*` tag (user decision).

**End** via the `session-handoff` skill.
