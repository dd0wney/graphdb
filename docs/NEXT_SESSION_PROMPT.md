# Next session prompt

> **Generated by**: `docs/SESSION_HANDOFF_2026-05-10-0236Z.md`
> **Singleton file**: every `session-handoff` skill invocation overwrites this. The latest handoff is the source.
> **Usage**: paste the block below to the next Claude Code session in this repo.

---

Deploy the graphdb coord instance per `docs/COORD_SETUP.md`, then pick up A8.2 using the new parallel-agent skills end-to-end: `work-claim` before starting, `integration-checkpoint` mid-work, `ci-status-triage` before merge.

This is the natural validation of the skills landed in PR #78 — they're scaffolding until coord deploys. If the skills produce friction, that's API feedback for `pkg/api/handlers_nodes` / `handlers_edges` / GraphQL surface.

Pre-flight before starting:

1. `GRAPHDB_COORD_URL` and `GRAPHDB_COORD_TOKEN` env vars set per `docs/COORD_SETUP.md`.
2. Coord instance reachable: `curl -fsS "${GRAPHDB_COORD_URL}/health"` returns 200.
3. Schema bootstrapped (uniqueness constraint + property indexes per `docs/COORD_SETUP.md` step 3).

End the session via the `session-handoff` skill, with a section on what worked / what didn't in the skill flow — that's the validation report this session's handoff is asking for.
