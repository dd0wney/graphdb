# GraphDB Multi-Environment Docker Configuration
# Copy this file to .env and fill in the values
#
# Usage:
#   cp .env.multi-env.example .env
#   # Edit .env with your values
#   docker compose -f docker-compose.multi-env.yml up -d

# Docker Image Configuration
DOCKER_REGISTRY=dd0wney
VERSION=latest

# Edition (community or enterprise)
GRAPHDB_EDITION=community

# Port Configuration
STAGING_PORT=8081
PRODUCTION_PORT=8080

# Staging Secrets
# Generate with: openssl rand -base64 32
STAGING_JWT_SECRET=your-staging-jwt-secret-change-me
STAGING_ADMIN_PASSWORD=staging-admin-password

# Production Secrets
# Generate with: openssl rand -base64 32
PRODUCTION_JWT_SECRET=your-production-jwt-secret-change-me
PRODUCTION_ADMIN_PASSWORD=production-admin-password

# Note on API Keys:
# - Staging (GRAPHDB_ENV=test) accepts gdb_test_* prefixed API keys
# - Production (GRAPHDB_ENV=production) requires gdb_live_* prefixed API keys
